#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int x1, x2, x3;
    std::cin >> x1 >> x2 >> x3;
        int ans = std::max({x1, x2, x3}) - std::min({x1, x2, x3});
    std::cout << ans << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}