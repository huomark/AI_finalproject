#include <bits/stdc++.h>
 using i64 = long long;
using u64 = unsigned long long;
using u32 = unsigned;
 using i128 = __int128;
using u128 = unsigned __int128;
 void solve() {
    std::string s;
    std::cin >> s;
        std::sort(s.begin(), s.end());
        for (int i = 9; i >= 0; i--) {
        int x = std::lower_bound(s.begin(), s.end(), '0' + i) - s.begin();
        std::cout << s[x];
        s.erase(s.begin() + x);
    }
    std::cout << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}