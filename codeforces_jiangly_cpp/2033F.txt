#include <bits/stdc++.h>
 using i64 = long long;
using u64 = unsigned long long;
using u32 = unsigned;
 constexpr u32 P = 1000000007;
 void solve() {
    i64 n;
    int k;
    std::cin >> n >> k;
        int a = 0, b = 1 % k;
    i64 len = 0;
    do {
        len++;
        std::tie(a, b) = std::pair(b, (a + b) % k);
    } while (a);
        std::cout << len * (n % P) % P << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}