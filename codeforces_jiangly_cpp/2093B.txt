#include <bits/stdc++.h>
 using i64 = long long;
using u64 = unsigned long long;
using u32 = unsigned;
using u128 = unsigned __int128;
 void solve() {
    std::string n;
    std::cin >> n;
        int i = n.size() - 1;
    while (n[i] == '0') {
        i--;
    }
        int ans = n.size() - 1 - std::count(n.begin(), n.begin() + i, '0');
    std::cout << ans << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}