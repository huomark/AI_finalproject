#include <bits/stdc++.h>
 using i64 = long long;
using u64 = unsigned long long;
using u32 = unsigned;
using u128 = unsigned __int128;
 void solve() {
    int n;
    std::cin >> n;
        if (n <= 4) {
        std::cout << -1 << "\n";
        return;
    }
        std::vector<int> p;
    for (int i = 7; i <= n; i += 2) {
        p.push_back(i);
    }
    p.push_back(1);
    p.push_back(3);
    p.push_back(5);
    p.push_back(4);
    p.push_back(2);
    for (int i = 6; i <= n; i += 2) {
        p.push_back(i);
    }
    for (auto x : p) {
        std::cout << x << " \n"[x == p.back()];
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}