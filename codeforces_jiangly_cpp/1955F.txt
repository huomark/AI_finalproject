#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int p[5];
    int ans = 0;
    for (int i = 1; i <= 4; i++) {
        std::cin >> p[i];
        ans += p[i] / 2;
    }
        ans += p[1] * p[2] * p[3] % 2;
    std::cout << ans << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}