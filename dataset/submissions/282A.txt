#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int n;
    std::cin >> n;
        int x = 0;
    while (n--) {
        std::string s;
        std::cin >> s;
                if (s.find('+') != -1) {
            x++;
        } else {
            x--;
        }
    }
        std::cout << x << "\n";
        return 0;
}