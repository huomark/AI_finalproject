#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int p1, p2, p3;
    std::cin >> p1 >> p2 >> p3;
        if ((p1 + p2 + p3) % 2 == 1) {
        std::cout << -1 << "\n";
    } else {
        std::cout << std::min((p1 + p2 + p3) / 2, p1 + p2) << "\n";
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}