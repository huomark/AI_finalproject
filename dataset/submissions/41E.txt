#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int n;
    std::cin >> n;
        int m = n/2;
        std::cout << m * (n-m) << "\n";
    for (int i = 1; i <= m; i++) {
        for (int j = m+1; j <= n; j++) {
            std::cout << i << " " << j << "\n";
        }
    }
        return 0;
}