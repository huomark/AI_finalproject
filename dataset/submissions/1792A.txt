#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n;
    std::cin >> n;
        int cnt1 = 0;
    for (int i = 0; i < n; i++) {
        int x;
        std::cin >> x;
        cnt1 += (x == 1);
    }
        int ans = n - cnt1 / 2;
    std::cout << ans << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}