#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n;
    std::cin >> n;
        if (n % 2 == 1) {
        std::cout << "NO\n";
        return;
    }
        std::cout << "YES\n";
    for (int i = 0; i < n; i++) {
        std::cout << char('A' + i / 2);
    }
    std::cout << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}