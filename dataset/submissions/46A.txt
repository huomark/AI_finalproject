#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int n;
    std::cin >> n;
        int x = 0;
    for (int i = 1; i < n; i++) {
        x += i;
        std::cout << x % n + 1 << " \n"[i == n-1];
    }
        return 0;
}