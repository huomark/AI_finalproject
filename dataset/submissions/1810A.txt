#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n;
    std::cin >> n;
        bool ok = false;
    for (int i = 1; i <= n; i++) {
        int x;
        std::cin >> x;
                if (x <= i) {
            ok = true;
        }
    }
        std::cout << (ok ? "YES" : "NO") << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}