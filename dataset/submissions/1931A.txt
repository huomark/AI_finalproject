#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n;
    std::cin >> n;
    n -= 3;
        for (int i = 1; i <= 3; i++) {
        int x = std::max(0, n - (3 - i) * 25);
        n -= x;
        std::cout << char('a' + x);
    }
    std::cout << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}