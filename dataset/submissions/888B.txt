#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int n;
    std::cin >> n;
        std::map<char, int> cnt;
    std::string s;
    std::cin >> s;
        for (auto c : s) {
        cnt[c]++;
    }
        int ans = 2 * std::min(cnt['L'], cnt['R']) + 2 * std::min(cnt['U'], cnt['D']);
    std::cout << ans << "\n";
        return 0;
}