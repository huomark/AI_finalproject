#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n, x1, y1, x2, y2;
    std::cin >> n >> x1 >> y1 >> x2 >> y2;
        int a = std::min({x1, n - x1 + 1, y1, n - y1 + 1});
    int b = std::min({x2, n - x2 + 1, y2, n - y2 + 1});
        std::cout << std::abs(a - b) << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}