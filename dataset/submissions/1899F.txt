#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n, q;
    std::cin >> n >> q;
        int u = n;
    int v1 = n - 1;
        for (int i = 1; i < n; i++) {
        std::cout << i << " " << i + 1 << "\n";
    }
        while (q--) {
        int d;
        std::cin >> d;
                int v2 = d;
        if (v1 == v2) {
            std::cout << "-1 -1 -1\n";
        } else {
            std::cout << u << " " << v1 << " " << v2 << "\n";
            v1 = v2;
        }
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}