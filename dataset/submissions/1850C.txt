#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    for (int i = 0; i < 8; i++) {
        std::string s;
        std::cin >> s;
                for (auto c : s) {
            if (std::isalpha(c)) {
                std::cout << c;
            }
        }
    }
    std::cout << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}