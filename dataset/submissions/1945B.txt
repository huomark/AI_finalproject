#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    i64 a, b, m;
    std::cin >> a >> b >> m;
    m++;
        i64 ans = (m + a - 1) / a + (m + b - 1) / b;
    std::cout << ans << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}