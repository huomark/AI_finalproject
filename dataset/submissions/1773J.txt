#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n, m;
    std::cin >> n >> m;
        std::string a, b;
    std::cin >> a >> b;
    std::reverse(b.begin(), b.end());
        a += b;
        int cnt = 0;
    for (int i = 0; i < a.size() - 1; i++) {
        if (a[i] == a[i + 1]) {
            cnt++;
        }
    }
        if (cnt <= 1) {
        std::cout << "YES\n";
    } else {
        std::cout << "NO\n";
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}