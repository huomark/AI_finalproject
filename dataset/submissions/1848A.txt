#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n, m, k;
    std::cin >> n >> m >> k;
        int x, y;
    std::cin >> x >> y;
        bool caught = false;
    for (int i = 0; i < k; i++) {
        int a, b;
        std::cin >> a >> b;
                if ((x + y + a + b) % 2 == 0) {
            caught = true;
        }
    }
        if (caught) {
        std::cout << "NO\n";
    } else {
        std::cout << "YES\n";
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}