#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    std::string s;
    std::cin >> s;
        int x = std::min_element(s.begin(), s.end()) - s.begin();
    std::cout << s[x] << " " << s.substr(0, x) + s.substr(x + 1) << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}