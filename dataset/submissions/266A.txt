#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int n;
    std::cin >> n;
        std::string s;
    std::cin >> s;
        int ans = 0;
    for (int i = 1; i < n; i++) {
        ans += (s[i - 1] == s[i]);
    }
        std::cout << ans << "\n";
        return 0;
}