#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int n;
    std::cin >> n;
        std::set<int> s;
    for (int i = 0; i < n; i++) {
        int x;
        std::cin >> x;
        s.insert(x);
    }
        if (s.size() == 1) {
        std::cout << "NO\n";
    } else {
        std::cout << *std::next(s.begin()) << "\n";
    }
        return 0;
}