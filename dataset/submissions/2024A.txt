#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int a, b;
    std::cin >> a >> b;
        if (a >= b) {
        std::cout << a << "\n";
    } else if (2 * a < b) {
        std::cout << 0 << "\n";
    } else {
        std::cout << 2 * a - b << "\n";
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
     int t;
    std::cin >> t;
     while (t--) {
        solve();
    }
     return 0;
}