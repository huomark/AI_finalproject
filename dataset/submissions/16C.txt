#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int a, b, x, y;
    std::cin >> a >> b >> x >> y;
        int g = std::gcd(x, y);
    x /= g, y /= g;
        int t = std::min(a / x, b / y);
    std::cout << t * x << " " << t * y << "\n";
        return 0;
}