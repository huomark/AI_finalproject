#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n;
    std::cin >> n;
        int a = 1023, o = 0;
    for (int i = 0; i < n; i++) {
        int x;
        std::cin >> x;
        a &= x;
        o |= x;
    }
        std::cout << o - a << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}