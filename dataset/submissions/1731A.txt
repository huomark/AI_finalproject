#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n;
    std::cin >> n;
        i64 ans = 1;
    for (int i = 0; i < n; i++) {
        int x;
        std::cin >> x;
        ans *= x;
    }
    ans += n - 1;
    ans *= 2022;
        std::cout << ans << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}