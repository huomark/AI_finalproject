#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        std::string s[3];
    std::cin >> s[0] >> s[1] >> s[2];
        for (int x = 0; x < 3; x++) {
        for (int y = 0; y < 3; y++) {
            if (s[x][y] != s[2 - x][2 - y]) {
                std::cout << "NO\n";
                return 0;
            }
        }
    }
    std::cout << "YES\n";
        return 0;
}