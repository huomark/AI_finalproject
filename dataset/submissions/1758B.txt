#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n;
    std::cin >> n;
        if (n % 2 == 1) {
        for (int i = 1; i <= n; i++) {
            std::cout << 1 << " \n"[i == n];
        }
    } else {
        for (int i = 1; i <= n - 2; i++) {
            std::cout << 2 << " ";
        }
        std::cout << 1 << " " << 3 << "\n";
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}