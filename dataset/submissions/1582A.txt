#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int a, b, c;
    std::cin >> a >> b >> c;
        if (c % 2 == 1 && a == 0 && b == 0) {
        std::cout << 3 << "\n";
    } else if ((c % 2 == 1 && a == 1 && b == 0) || (c % 2 == 0 && a == 0 && b == 1)) {
        std::cout << 2 << "\n";
    } else if ((a + c) % 2 == 1) {
        std::cout << 1 << "\n";
    } else {
        std::cout << 0 << "\n";
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}