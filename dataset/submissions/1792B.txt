#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int a, b, c, d;
    std::cin >> a >> b >> c >> d;
        if (a == 0) {
        std::cout << 1 << "\n";
    } else {
        std::cout << a + std::min(b, c) * 2 + std::min(a + 1, std::abs(b - c) + d) << "\n";
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}