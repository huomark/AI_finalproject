#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int n;
    std::cin >> n;
        std::vector<int> a(n);
    for (int i = 0; i < n; i++) {
        std::cin >> a[i];
    }
    std::sort(a.begin(), a.end());
        if (std::count(a.begin(), a.begin() + n / 2 + 1, a[0]) == n / 2 + 1) {
        std::cout << "Bob\n";
    } else {
        std::cout << "Alice\n";
    }
        return 0;
}