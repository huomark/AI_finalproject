#include <bits/stdc++.h>
 using ll = long long;
using namespace std;
#define For(i,l,r) for(int i=(int)(l);i<=(int)(r);i++)
#define PI pair<int,int>
#define mp make_pair
#define fi first
#define se second
#define pb push_back
const int K=31;
int n,a[K];
const int mod=998244353;
ll ksm(ll a,int b){
    int ans=1;
    for(;b;b>>=1){
        if(b&1)ans=ans*a%mod;
        a=a*a%mod;
    }
    return ans;
}
int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
     int t=1;
    while(t--){
        cin>>n;
        int t,rk=0;
        ll sum=0;
        For(i,1,n){
            cin>>t;
            t|=1<<30;
            for(int j=K-1;j>=0;j--)if(t>>j&1){
                if(a[j]){
                    t^=a[j];
                }else{
                    a[j]=t; break;
                }
            }
            if(t==0){
                sum+=i-1-rk-1; rk++;
            }else{
                sum+=i-1;
            }
                    }
        cout<<ksm(2,sum%(mod-1))<<endl;
    }
     return 0;
}