#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    std::string s;
    std::cin >> s;
        if (s != std::string(s.rbegin(), s.rend())) {
        std::cout << s.size() << "\n";
    } else if (s == std::string(s.size(), s[0])) {
        std::cout << -1 << "\n";
    } else {
        std::cout << s.size() - 1 << "\n";
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}