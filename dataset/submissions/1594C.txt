#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n;
    std::cin >> n;
        char c;
    std::cin >> c;
        std::string s;
    std::cin >> s;
        if (s == std::string(n, c)) {
        std::cout << 0 << "\n";
        return;
    }
        for (int i = 1; i <= n; i++) {
        bool ok = true;
        for (int j = i; j <= n; j += i) {
            if (s[j - 1] != c) {
                ok = false;
            }
        }
        if (ok) {
            std::cout << 1 << "\n";
            std::cout << i << "\n";
            return;
        }
    }
        std::cout << 2 << "\n";
    std::cout << n - 1 << " " << n << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}