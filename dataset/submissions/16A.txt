#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int n, m;
    std::cin >> n >> m;
        std::vector<std::string> s(n);
    for (int i = 0; i < n; i++) {
        std::cin >> s[i];
        if (s[i] != std::string(m, s[i][0])) {
            std::cout << "NO\n";
            return 0;
        }
        if (i > 0 && s[i] == s[i - 1]) {
            std::cout << "NO\n";
            return 0;
        }
    }
    std::cout << "YES\n";
        return 0;
}