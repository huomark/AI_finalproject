#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n, m;
    std::cin >> n >> m;
        std::string x, s;
    std::cin >> x >> s;
        int ans = 0;
    while (x.find(s) == -1 && (ans == 0 || x.size() < 2 * m)) {
        x = x + x;
        ans++;
    }
        if (x.find(s) == -1) {
        std::cout << -1 << "\n";
    } else {
        std::cout << ans << "\n";
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}