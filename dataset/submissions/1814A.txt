#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    i64 n, k;
    std::cin >> n >> k;
        if (n % 2 == 0 || (k % 2 == 1 && n >= k)) {
        std::cout << "YES\n";
    } else {
        std::cout << "NO\n";
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}