#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int l, r, x;
    std::cin >> l >> r >> x;
        int a, b;
    std::cin >> a >> b;
        if (a == b) {
        std::cout << 0 << "\n";
        return;
    }
        if (std::abs(a - b) >= x) {
        std::cout << 1 << "\n";
        return;
    }
        if (a - l >= x && b - l >= x) {
        std::cout << 2 << "\n";
        return;
    }
        if (r - a >= x && r - b >= x) {
        std::cout << 2 << "\n";
        return;
    }
        if (a - l >= x && r - b >= x) {
        std::cout << 3 << "\n";
        return;
    }
        if (b - l >= x && r - a >= x) {
        std::cout << 3 << "\n";
        return;
    }
        std::cout << -1 << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}