#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int x, y;
    std::cin >> x >> y;
        int ans = std::max((y + 1) / 2, (x + 4 * y + 14) / 15);
    std::cout << ans << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}