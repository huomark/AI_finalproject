#include <bits/stdc++.h>
 using i64 = long long;
 int f(int x) {
    return x ? f(x / 10) + x % 10 : 0;
}
 void solve() {
    int x, k;
    std::cin >> x >> k;
        while (f(x) % k != 0) {
        x++;
    }
        std::cout << x << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}