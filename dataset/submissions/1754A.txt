#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n;
    std::cin >> n;
        std::string s;
    std::cin >> s;
        std::reverse(s.begin(), s.end());
        int cur = 0;
    for (auto c : s) {
        if (c == 'A') {
            cur++;
        } else {
            cur--;
        }
        if (cur < 0) {
            std::cout << "No\n";
            return;
        }
    }
        std::cout << "Yes\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}