#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int n;
    std::cin >> n;
        int cnt[3] {};
    for (int i = 0; i < n; i++) {
        int x;
        std::cin >> x;
        cnt[x % 3]++;
    }
        int ans = std::min(cnt[1], cnt[2]) + cnt[0] / 2;
    std::cout << ans << "\n";
        return 0;
}