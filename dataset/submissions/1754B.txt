#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n;
    std::cin >> n;
        int c = n / 2;
    for (int i = 1; i <= c; i++) {
        std::cout << i + c << " ";
        std::cout << i << " ";
    }
    if (n % 2 == 1) {
        std::cout << n << "\n";
    }
    std::cout << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}