#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        std::string a, b;
    std::cin >> a >> b;
        if (a.size() == b.size() && (a.find('1') != -1) == (b.find('1') != -1)) {
        std::cout << "YES\n";
    } else {
        std::cout << "NO\n";
    }
        return 0;
}