#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        std::string s;
    std::cin >> s;
        int m;
    std::cin >> m;
        for (int i = 0; i < m; i++) {
        int l, r, k;
        std::cin >> l >> r >> k;
                l--;
        int len = r - l;
        k %= len;
        std::rotate(s.begin() + l, s.begin() + r - k, s.begin() + r);
    }
    std::cout << s << "\n";
        return 0;
}