#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int n;
    std::cin >> n;
        std::vector<int> a(n);
    int cnt[2] {};
    for (int i = 0; i < n; i++) {
        std::cin >> a[i];
        cnt[a[i] % 2] += 1;
    }
        for (int i = 0; i < n; i++) {
        if (cnt[a[i] % 2] == 1) {
            std::cout << i + 1 << "\n";
        }
    }
        return 0;
}