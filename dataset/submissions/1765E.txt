#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n, a, b;
    std::cin >> n >> a >> b;
        if (a > b) {
        std::cout << 1 << "\n";
    } else {
        std::cout << (n + a - 1) / a << "\n";
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}