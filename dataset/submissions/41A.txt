#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        std::string s, t;
    std::cin >> s >> t;
        if (s == std::string(t.rbegin(), t.rend())) {
        std::cout << "YES\n";
    } else {
        std::cout << "NO\n";
    }
        return 0;
}