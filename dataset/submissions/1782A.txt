#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int w, d, h;
    int a, b, f, g;
    std::cin >> w >> d >> h;
    std::cin >> a >> b >> f >> g;
        auto ans = std::min({a + f + std::abs(b - g), 2 * w - a - f + std::abs(b - g), b + g + std::abs(a - f), 2 * d - b - g + std::abs(a - f)}) + h;
    std::cout << ans << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}