#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int k, n;
    std::cin >> k >> n;
        std::cout << 1;
    int x = 1;
    for (int i = 2; i <= k; i++) {
        int a = std::min(n - (k - i), x + i - 1);
        std::cout << " " << a;
        x = a;
    }
    std::cout << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}