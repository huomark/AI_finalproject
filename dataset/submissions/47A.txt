#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int n;
    std::cin >> n;
        int m = std::sqrt(2 * n);
    if (m * (m+1) / 2 == n) {
        std::cout << "YES\n";
    } else {
        std::cout << "NO\n";
    }
        return 0;
}