#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int n;
    std::cin >> n;
        std::vector<int> s(n), e(n);
    bool ok = true;
    for (int i = 0; i < n; i++) {
        std::cin >> s[i] >> e[i];
        if (i > 0 && s[i] >= s[0] && e[i] >= e[0]) {
            ok = false;
        }
    }
    std::cout << (ok ? s[0] : -1) << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}