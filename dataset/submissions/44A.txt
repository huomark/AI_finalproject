#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int n;
    std::cin >> n;
        std::set<std::pair<std::string, std::string>> s;
    for (int i = 0; i < n; i++) {
        std::string a, b;
        std::cin >> a >> b;
        s.emplace(a, b);
    }
    std::cout << s.size() << "\n";
        return 0;
}