#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    std::string s;
    std::cin >> s;
        for (char x = 'a'; x <= 'h'; x++) {
        for (char y = '1'; y <= '8'; y++) {
            if ((x == s[0]) ^ (y == s[1])) {
                std::cout << x << y << "\n";
            }
        }
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}