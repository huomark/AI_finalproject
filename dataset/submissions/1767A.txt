#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    int x[3], y[3];
    for (int i = 0; i < 3; i++) {
        std::cin >> x[i] >> y[i];
    }
        if (std::set(x, x + 3).size() == 2 && std::set(y, y + 3).size() == 2) {
        std::cout << "NO\n";
    } else {
        std::cout << "YES\n";
    }
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}