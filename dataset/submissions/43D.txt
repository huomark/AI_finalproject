#include <bits/stdc++.h>
 using i64 = long long;
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int n, m;
    std::cin >> n >> m;
        if (n % 2 == 0 && (m > 1 || n == 2)) {
        std::cout << 0 << "\n";
        for (int i = 1; i <= n; i++) {
            std::cout << i << " " << 1 << "\n";
        }
        for (int i = n; i; i--) {
            if (i % 2) {
                for (int j = m; j > 1; j--) {
                    std::cout << i << " " << j << "\n";
                }
            } else {
                for (int j = 2; j <= m; j++) {
                    std::cout << i << " " << j << "\n";
                }
            }
        }
        std::cout << 1 << " " << 1 << "\n";
    } else if (m % 2 == 0 && (n > 1 || m == 2)) {
        std::cout << 0 << "\n";
        for (int i = 1; i <= m; i++) {
            std::cout << 1 << " " << i << "\n";
        }
        for (int i = m; i; i--) {
            if (i % 2) {
                for (int j = n; j > 1; j--) {
                    std::cout << j << " " << i << "\n";
                }
            } else {
                for (int j = 2; j <= n; j++) {
                    std::cout << j << " " << i << "\n";
                }
            }
        }
        std::cout << 1 << " " << 1 << "\n";
    } else {
        std::cout << 1 << "\n";
        std::cout << n << " " << m << " " << 1 << " " << 1 << "\n";
        for (int i = 1; i <= n; i++) {
            if (i % 2) {
                for (int j = 1; j <= m; j++) {
                    std::cout << i << " " << j << "\n";
                }
            } else {
                for (int j = m; j; j--) {
                    std::cout << i << " " << j << "\n";
                }
            }
        }
        std::cout << 1 << " " << 1 << "\n";
    }
        return 0;
}