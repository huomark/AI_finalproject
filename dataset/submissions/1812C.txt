#include <bits/stdc++.h>
 using i64 = long long;
 const char *s = "31415926535897932384626433832795";
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        for (int i = 0; i < t; i++) {
        int n = s[i] - '0';
        int ans = 1;
        while (n--) {
            int x;
            std::cin >> x;
            ans *= x;
        }
        std::cout << ans << "\n";
    }
        return 0;
}