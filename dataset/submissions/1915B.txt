#include <bits/stdc++.h>
 using i64 = long long;
 void solve() {
    char ans = 'A' ^ 'B' ^ 'C' ^ '?';
    for (int i = 0; i < 9; i++) {
        char c;
        std::cin >> c;
        ans ^= c;
    }
    std::cout << ans << "\n";
}
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        int t;
    std::cin >> t;
        while (t--) {
        solve();
    }
        return 0;
}