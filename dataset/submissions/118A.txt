#include <bits/stdc++.h>
 using i64 = long long;
 std::string t = "AOYEUI";
 int main() {
    std::ios::sync_with_stdio(false);
    std::cin.tie(nullptr);
        std::string s;
    std::cin >> s;
        for (auto &c : s) {
        if (t.find(std::toupper(c)) == -1) {
            std::cout << '.' << char(std::tolower(c));
        }
    }
    std::cout << "\n";
        return 0;
}